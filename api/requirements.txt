# Runtime dependencies for the standalone BDD-SKU API container.
# az-scout is NOT needed here – it's only required for plugin registration.
fastapi>=0.115
uvicorn[standard]>=0.30
psycopg[binary]>=3.2
psycopg_pool>=3.2
azure-identity>=1.17
